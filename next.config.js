/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    images: {
      domains: ['nextui-docs-v2.vercel.app'], // Hata veren görüntü kaynağının ana bilgisini buraya ekleyin
      domains: ['images.unsplash.com'],

    },
  };
  
